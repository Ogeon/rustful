[package]

name = "rustful"
version = "0.1.0-pre"
authors = ["hello@erikhedvall.nu"]

[lib]

name = "rustful"
path = "src/lib.rs"

[features]
nightly = ["hyper/nightly", "anymap/nightly"]

[dependencies]
hyper = "0.3"
time = "0.1"
url = "0.2"
anymap = "0.10"

[dev-dependencies]
tempdir = "0.3"
